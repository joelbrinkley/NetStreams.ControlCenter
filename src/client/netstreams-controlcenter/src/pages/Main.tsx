import React, { FC } from "react";
import {Container, Row} from "react-bootstrap";

export const Main: FC = () => {
  return (
    <Container>
      <Row>
        <p> Main Page</p>
      </Row>
    </Container>
  );
};
